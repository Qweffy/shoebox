# Cloud benchmark adapters — all free tiers. M1 (OCR ledger CLI) needs NONE of these.
# Gemini: https://aistudio.google.com/app/apikey  (no credit card)
GEMINI_API_KEY=
# Groq: reuse your existing key  (free tier)
GROQ_API_KEY=
# Hugging Face WRITE token — only for M5 (publishing the fine-tuned model)
# https://huggingface.co/settings/tokens
HF_TOKEN=
